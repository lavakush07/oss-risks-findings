// Placeholder module so CI/tools can treat this folder as a Go project.
// VulnCheck golang PURLs for abandoned / hijackable-repository are historical;
// they may not "go get" today — query VulnCheck by PURL (see ../../vulncheck_purl_catalog.json).
package main

func main() {}
